CREATE TABLE pessoa (
                        id IDENTITY PRIMARY KEY,
                        nome VARCHAR(100),
                        cpf VARCHAR(11),
                        email VARCHAR(100),
                        idade INT
);